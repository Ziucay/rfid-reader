# rfid-reader

This device reads data from RFID scanner via IButtton protocol, then maps card id to it's internal id, and sends it to serial. To change it's internal id, there's a screen and an encoder. Internal ID's are stored internally and persist between restarts.

data folder contains an example of data stored in memory. 

model folder has box for the prototype board.
